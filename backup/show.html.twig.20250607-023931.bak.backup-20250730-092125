{% extends 'base.html.twig' %}

{# {% block title %} #}📢 PopUp Details{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">📢 PopUp Details</h4>
            <div>
                <a href="{{ path('app_pop_up_manager_edit', {'id': pop_up_manager.id}) }}" class="btn btn-sm btn-warning me-2">✏️ Bearbeiten</a>
                <a href="{{ path('app_pop_up_manager_index') }}" class="btn btn-sm btn-outline-light">⬅️ Zurück</a>
                {{ include('pop_up_manager/_delete_form.html.twig') }}
            </div>
        </div>

        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-4">🆔 ID:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.id }}</dd>

                <dt class="col-sm-4">📝 Name:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.popupName }}</dd>

                <dt class="col-sm-4">📌 Status:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.popupStatus }}</dd>

                <dt class="col-sm-4">⏰ Ablauf:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.popupExpires ? pop_up_manager.popupExpires|date('d.m.Y H:i') : '–' }}</dd>

                <dt class="col-sm-4">🕑 Erstellt am:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.popupCreate ? pop_up_manager.popupCreate|date('d.m.Y H:i') : '–' }}</dd>

                <dt class="col-sm-4">👤 Erstellt von:</dt>
                <dd class="col-sm-8">{{ pop_up_manager.popupUser ?: '–' }}</dd>

                <dt class="col-sm-4">🧾 Beschreibung:</dt>
                <dd class="col-sm-8">
                    {% if pop_up_manager.popupDescription %}
                        <div>{{ pop_up_manager.popupDescription|raw }}</div>
                    {% else %}
                        <span class="text-muted">–</span>
                    {% endif %}
                </dd>

                <dt class="col-sm-4">💬 Shortcode:</dt>
                <dd class="col-sm-8">
                    <code class="d-block bg-light p-2 rounded text-break">
                        [[shortcode_button tag="help" id={{ pop_up_manager.id }} modal=true]]
                    </code>
                    <button class="btn btn-sm btn-outline-secondary mt-1"
                            onclick="navigator.clipboard.writeText('[[shortcode_button tag=&quot;help&quot; id={{ pop_up_manager.id }} modal=true]]')">
                        📋 In Zwischenablage kopieren
                    </button>
                </dd>

                <dt class="col-sm-4">💬 Inhalt:</dt>
                <dd class="col-sm-8">
                    {% if pop_up_manager.popupContent %}
                        <div class="readonly-display">
                            {{ pop_up_manager.popupContent|raw }}
                        </div>
                    {% else %}
                        <span class="text-muted">–</span>
                    {% endif %}
                </dd>
            </dl>


        </div>

        <div class="card-footer bg-transparent border-top-0 d-flex justify-content-end">
            
        </div>
    </div>
</div>
{% endblock %}

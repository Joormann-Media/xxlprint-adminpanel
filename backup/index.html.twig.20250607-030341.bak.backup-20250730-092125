{% extends 'base.html.twig' %}

{% block title %}🏢 Firmenübersicht{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h3 mb-0"><i class="bi bi-building"></i> FirmenEinstellungen</h1>
        {% if is_granted('ROLE_DEVELOPER') %}

            <a href="{{ path('app_company_new') }}" class="btn btn-success">
                <i class="bi bi-plus-circle"></i> Neue Firma
            </a>
        {% endif %}
        
    </div>

    <div class="table-responsive shadow-sm">
        <table class="table table-bordered table-striped align-middle">
            <thead class="table-primary text-center">
                <tr>
                    <th>Logo</th>
                    <th>Firma</th>
                    <th>Vorname</th>
                    <th>Name</th>
                    <th>Straße</th>
                    <th>Nr.</th>
                    <th>PLZ</th>
                    <th>Ort</th>
                    <th>Telefon</th>
                    <th>Fax</th>
                    <th>E-Mail</th>
                    <th>Web</th>
                    <th>St-Nr</th>
                    <th>USt-ID</th>
                    <th>Aktionen</th>
                </tr>
            </thead>
            <tbody>
                {% for company in companies %}
                    <tr>
                        <td class="text-center">
                            {% if company.companyLogo %}
                                <img src="/uploads/{{ company.companyLogo }}" alt="Firmenlogo" class="img-thumbnail" style="max-width: 80px;">
                            {% else %}
                                <span class="text-muted small">Kein Logo</span>
                            {% endif %}
                        </td>
                        <td>{{ company.companyname }}</td>
                        <td>{{ company.ceoprename }}</td>
                        <td>{{ company.ceoname }}</td>
                        <td>{{ company.street }}</td>
                        <td>{{ company.streetno }}</td>
                        <td>{{ company.zipcode }}</td>
                        <td>{{ company.city }}</td>
                        <td>{{ company.phone }}</td>
                        <td>{{ company.fax ?? '—' }}</td>
                        <td><a href="mailto:{{ company.email }}">{{ company.email }}</a></td>
                        <td>
                            {% if company.web %}
                                <a href="{{ company.web }}" target="_blank">{{ company.web|replace({'https://': '', 'http://': ''}) }}</a>
                            {% else %}
                                —
                            {% endif %}
                        </td>
                        <td>{{ company.taxno }}</td>
                        <td>{{ company.taxid }}</td>
                        <td class="text-center">
                            <a href="{{ path('app_company_show', {'id': company.id}) }}" class="btn btn-outline-primary btn-sm me-1" title="Anzeigen">
                                <i class="bi bi-eye"></i>
                            </a>
                            <a href="{{ path('app_company_edit', {'id': company.id}) }}" class="btn btn-outline-secondary btn-sm me-1" title="Bearbeiten">
                                <i class="bi bi-pencil"></i>
                            </a>
                            {{ include('company/_delete_form.html.twig', { company: company }) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="15" class="text-center text-muted py-4">Keine Firmen gefunden.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}

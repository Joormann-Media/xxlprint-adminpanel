{% extends 'base.html.twig' %}

{% block title %}AddressBook index{% endblock %}

{% block body %}
    <h1>AddressBook index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for address_book in address_books %}
            <tr>
                <td>{{ address_book.id }}</td>
                <td>{{ address_book.name }}</td>
                <td>
                    <a href="{{ path('app_address_book_show', {'id': address_book.id}) }}">show</a>
                    <a href="{{ path('app_address_book_edit', {'id': address_book.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_address_book_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{# {% block title %} #}Shortcode Test{% endblock %}

{% block body %}
<h1 class="mb-4">🔬 Shortcode-Testseite</h1>

<form method="get" class="mb-4">
    <label for="shortcodeInput" class="form-label fw-semibold">🎯 Test-Shortcode</label>
    <textarea id="shortcodeInput" name="shortcode" class="form-control mb-2" rows="2">{{ shortcode }}</textarea>
    <button class="btn btn-primary">🚀 Ausführen</button>
</form>

{% if error %}
    <div class="alert alert-danger"><strong>❌ Fehler:</strong> {{ error }}</div>
{% endif %}

{% if rendered %}
    <h5>🖼️ Vorschau</h5>
    <div class="border p-3 bg-light rounded mb-4 shadow-sm">
        {{ rendered|raw }}
    </div>
{% endif %}
{% endblock %}

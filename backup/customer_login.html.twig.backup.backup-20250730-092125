{% extends 'base.html.twig' %}

{# {% block title %} #}🔐 Kundenlogin{% endblock %}

{% block body %}
<div class="container my-5">
    <h1 class="mb-3">🔐 Kunden-Login</h1>
    <p class="text-muted">Melden Sie sich mit Ihrer E-Mail oder Ihrem Benutzernamen an.</p>

    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <form method="post" action="{{ path('app_customer_login') }}">
        <div class="mb-3">
            <label for="email">Benutzername oder E-Mail</label>
            <input type="text" name="_username" id="email" class="form-control"
                   value="{{ last_username }}" required autofocus>
        </div>

        <div class="mb-3">
            <label for="password">Passwort</label>
            <input type="password" name="_password" id="password" class="form-control" required>
        </div>

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}">

        <button type="submit" class="btn btn-primary w-100">
            <i class="bi bi-box-arrow-in-right me-1"></i> Einloggen
        </button>
    </form>
</div>
{% endblock %}

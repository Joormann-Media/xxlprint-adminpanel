{% extends 'base.html.twig' %}

{% block title %}SMS senden{% endblock %}

{% block body %}
<div class="container my-4">
    <h1>📤 SMS senden mit Vorlage: <strong>{{ sms_template.title }}</strong></h1>

    <div class="mb-3">
        <label class="form-label">Vorschau:</label>
        <div class="border rounded p-3 bg-light">{{ sms_template.message|nl2br }}</div>
    </div>

    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_label(form.recipient, 'Empfänger', {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.recipient, {'attr': {'class': 'form-control'}}) }}
            {{ form_errors(form.recipient) }}
        </div>
        <button type="submit" class="btn btn-success">📤 Jetzt senden</button>
        <a href="{{ path('app_sms_template_index') }}" class="btn btn-secondary">Zurück</a>
    {{ form_end(form) }}
</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Benutzergruppe bearbeiten{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">👥 Benutzergruppe bearbeiten</h4>
        </div>
        <div class="card-body">
            {{ form_start(form) }}

            <ul class="nav nav-tabs mb-3" id="groupTabs" role="tablist">
                <li class="nav-item">
                    <button class="nav-link active" id="tab-info" data-bs-toggle="tab" data-bs-target="#info" type="button" role="tab">📋 Basisdaten</button>
                </li>
                <li class="nav-item">
                    <button class="nav-link" id="tab-members" data-bs-toggle="tab" data-bs-target="#members" type="button" role="tab">👤 Mitglieder</button>
                </li>
                <li class="nav-item">
                    <button class="nav-link" id="tab-meta" data-bs-toggle="tab" data-bs-target="#meta" type="button" role="tab">⚙️ Metadaten</button>
                </li>
            </ul>

            <div class="tab-content">
                <div class="tab-pane fade show active" id="info" role="tabpanel">
                    <div class="row g-3 mt-2">
                        <div class="col-md-6">
                            {{ form_label(form.GroupName) }}
                            {{ form_widget(form.GroupName, {'attr': {'class': 'form-control'}}) }}
                        </div>


                        <div class="col-md-6">
                            {{ form_label(form.groupLogo) }}
                            {{ form_widget(form.groupLogo, {'attr': {'class': 'form-control'}}) }}
                            {% if user_group.groupLogo %}
                            <img src="{{ asset('user_groups/' ~ user_group.baseDir ~ '/avatar/' ~ user_group.groupLogo) }}"
                                 class="img-thumbnail mt-2"
                                 style="max-height: 64px; max-width: 64px;"
                                 alt="Gruppenlogo">
                        {% endif %}
                        </div>
                        <div class="col-md-12">
                            {{ form_label(form.GroupDescription) }}
                            {{ form_widget(form.GroupDescription) }}
                        </div>

                    </div>
                </div>

                <div class="tab-pane fade" id="members" role="tabpanel">
                    <div class="mt-3">
                        {{ form_label(form.groupMembers) }}
                        {{ form_widget(form.groupMembers, {'attr': {'class': 'form-select', 'multiple': 'multiple'}}) }}
                    </div>
                </div>

                <div class="tab-pane fade" id="meta" role="tabpanel">
                    <div class="row g-3 mt-2">
                        <div class="col-md-6">
                            {{ form_label(form.groupCreate) }}
                            {{ form_widget(form.groupCreate, {'attr': {'class': 'form-control', 'readonly': 'readonly', 'value': "now"|date("Y-m-d H:i:s")}}) }}
                        </div>
                        <div class="col-md-6">
                            {{ form_label(form.groupCReateBy) }}
                            {{ form_widget(form.groupCReateBy, {'attr': {'class': 'form-control', 'readonly': 'readonly','value': app.user.username}}) }}
                        </div>
                        <div class="col-md-6">
                            {{ form_label(form.baseDir) }}
                            {{ form_widget(form.baseDir, {'attr': {'class': 'form-control', 'readonly': 'readonly'}}) }}
                        </div>

                    </div>
                </div>
                
            </div>

            <div class="text-end mt-4">
                <button class="btn btn-primary">💾 Speichern</button>
                <a href="{{ path('app_user_groups_index') }}" class="btn btn-outline-secondary ms-2">⬅️ Zurück</a>
            </div>

            {{ form_end(form) }}
        </div>
    </div>
</div>

<script>
    const triggerTabList = document.querySelectorAll('#groupTabs button');
    triggerTabList.forEach(triggerEl => {
        const tabTrigger = new bootstrap.Tab(triggerEl);
        triggerEl.addEventListener('click', event => {
            event.preventDefault();
            tabTrigger.show();
        });
    });
</script>
{% endblock %}

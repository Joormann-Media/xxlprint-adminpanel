{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
<div class="login_background">
    {% for label, messages in app.flashes %}
    {% for message in messages %}
    <div class="alert alert-{{ label == 'success' ? 'success' : (label == 'danger' ? 'danger' : 'info') }} alert-dismissible fade show" role="alert">
    {{ message }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    {% endfor %}
{% endfor %}
<div class="container_center">
        <form method="post">
        {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        
        {% endif %}

<div class="container_center">
    <img src="/gfx/Joormann_Media_Logo2024.png" alt="Joormann-Media - Full Stack Web Agency" class="logo-formular"><br>
        <label for="username">🔤 Benutzername, ✉️ E-Mail oder <br>📱 Handy- oder Kundennummer</label>
        <input type="text" value="{{ last_username }}" name="_username" id="username" class="form-control Custom-css-input" autocomplete="username" required autofocus>
        <label for="password">Ihr Passwort:</label>
        <input type="password" name="_password" id="password" class="form-control Custom-css-input" autocomplete="current-password" required><br />

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        <button class="btn btn-lg btn-primary custom-btn" type="submit">
            LogIn
        </button><br>
        <a href="{{ path('app_register') }}">Registrieren</a>
    </div>
    </form>

</div>
</div>
{% endblock %}

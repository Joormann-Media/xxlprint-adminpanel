{% extends 'base.html.twig' %}

{% block title %}Benutzergruppe anzeigen{% endblock %}

{% block body %}
<div class="container py-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">👥 Benutzergruppe: {{ user_group.GroupName }}</h4>
        </div>
        <div class="card-body">

            {% if user_group.groupLogo %}
                <div class="text-center mb-4">
                    <img src="{{ asset('user_groups/' ~ user_group.baseDir ~ '/avatar/' ~ user_group.groupLogo) }}"
                         alt="Gruppenlogo"
                         class="img-thumbnail"
                         style="max-height: 100px;">
                </div>
            {% endif %}

            <div class="row mb-3">
                <div class="col-md-6">
                    <strong>ID:</strong><br>
                    {{ user_group.id }}
                </div>
                <div class="col-md-6">
                    <strong>Erstellt am:</strong><br>
                    {{ user_group.groupCreate ? user_group.groupCreate|date('d.m.Y H:i') : 'N/A' }}
                </div>
            </div>

            <div class="mb-3">
                <strong>Beschreibung:</strong><br>
                <div class="border p-2 bg-light rounded">
                    {{ user_group.GroupDescription|raw }}
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <strong>Erstellt von:</strong><br>
                    {{ user_group.groupCReateBy }}
                </div>
                <div class="col-md-6">
                    <strong>Verzeichnis:</strong><br>
                    <code>{{ user_group.baseDir }}</code>
                </div>
            </div>

        </div>
        <div class="card-footer text-end">
            <a href="{{ path('app_user_groups_edit', {'id': user_group.id}) }}" class="btn btn-warning">
                ✏️ Bearbeiten
            </a>
            {{ include('user_groups/_delete_form.html.twig') }}
            <a href="{{ path('app_user_groups_index') }}" class="btn btn-outline-secondary ms-2">
                ⬅️ Zurück zur Übersicht
            </a>
        </div>
    </div>
</div>
{% endblock %}

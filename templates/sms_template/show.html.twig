{# templates/sms_template/show.html.twig #}
{% extends 'base.html.twig' %}



{% block body %}
    <div class="container my-5">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                <h5 class="mb-0">SMS-Vorlage: {{ sms_template.title }}</h5>
                <div>
                    <a href="{{ path('app_sms_template_edit', {'id': sms_template.id}) }}" class="btn btn-sm btn-light me-2">Bearbeiten</a>
                    <a href="{{ path('app_sms_template_index') }}" class="btn btn-sm btn-outline-light">Zurück</a>
                    <form method="post" action="{{ path('app_sms_template_delete', {'id': sms_template.id}) }}" class="d-inline" onsubmit="return confirm('❗️Diese Vorlage wirklich löschen?');">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sms_template.id) }}">
                        <button class="btn btn-sm btn-danger">🗑️ Löschen</button>
                </div>
            </div>
            <div class="card-body">
                <dl class="row">
                    <dt class="col-sm-3">Titel</dt>
                    <dd class="col-sm-9">{{ sms_template.title }}</dd>

                    <dt class="col-sm-3">Nachricht</dt>
                    <dd class="col-sm-9"><pre class="mb-0">{{ sms_template.message }}</pre></dd>

                    <dt class="col-sm-3">Erstellt von</dt>
                    <dd class="col-sm-9">{{ sms_template.createdBy }}</dd>

                    <dt class="col-sm-3">Erstellt am</dt>
                    <dd class="col-sm-9">{{ sms_template.createdAt ? sms_template.createdAt|date('d.m.Y H:i') : '–' }}</dd>
                </dl>
            </div>
        </div>
    </div>
{% endblock %}

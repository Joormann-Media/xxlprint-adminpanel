<form method="post" action="{{ path('app_sms_template_delete', {'id': sms_template.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sms_template.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}


{% block body %}
<div class="container my-4">
    {% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{{ label }} alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}
{% endfor %}

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">📨 SMS-Vorlagen</h1>
        <a href="{{ path('app_sms_template_new') }}" class="btn btn-primary">
            ➕ Neue Vorlage
        </a>
    </div>

    {% if sms_templates is empty %}
        <div class="alert alert-warning">
            🚫 Keine SMS-Vorlagen gefunden.
        </div>
    {% else %}
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            {% for sms_template in sms_templates %}
                <div class="col">
                    <div class="card shadow-sm h-100">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">{{ sms_template.title }}</h5>
                            <p class="card-text text-muted small mb-2">
                                {{ sms_template.createdAt|date('d.m.Y H:i') }} von {{ sms_template.createdBy }}
                            </p>
                            <p class="card-text">{{ sms_template.message|e }}</p>
                            <div class="mt-auto">
                                <a href="{{ path('app_sms_template_show', {'id': sms_template.id}) }}" class="btn btn-sm btn-outline-primary me-2">
                                    🔍 Anzeigen
                                </a>
                                <a href="{{ path('app_sms_template_edit', {'id': sms_template.id}) }}" class="btn btn-sm btn-outline-secondary">
                                    ✏️ Bearbeiten
                                </a>
                                <form method="post" action="{{ path('app_sms_template_delete', {'id': sms_template.id}) }}" class="d-inline" onsubmit="return confirm('❗️Diese Vorlage wirklich löschen?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sms_template.id) }}">
                                    <button class="btn btn-sm btn-outline-danger">🗑️ Löschen</button>
                                </form>
                                <a href="{{ path('app_sms_template_send', {'id': sms_template.id}) }}"
   class="btn btn-sm btn-outline-success">
    📤 Senden
</a>

                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
</div>
{% endblock %}

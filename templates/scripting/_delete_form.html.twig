<form method="post" action="{{ path('app_scripting_delete', {'id': scripting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scripting.id) }}">
    <button class="btn">Delete</button>
</form>

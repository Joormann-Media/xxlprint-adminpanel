{% extends 'base.html.twig' %}


{% block body %}
<div class="container py-5">
    <div class="card shadow-sm">
        <div class="card-header bg-warning text-dark">
            <h3 class="mb-0">🔐 PIN zurücksetzen</h3>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.pin) }}
                    {{ form_widget(form.pin, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(form.pin) }}
                </div>
                <button type="submit" class="btn btn-primary">💾 PIN speichern</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}

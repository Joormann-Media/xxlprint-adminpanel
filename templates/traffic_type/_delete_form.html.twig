<form method="post" action="{{ path('app_traffic_type_delete', {'id': traffic_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traffic_type.id) }}">
    <button class="btn">Delete</button>
</form>

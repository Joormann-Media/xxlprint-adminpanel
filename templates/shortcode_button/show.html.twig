{% extends 'base.html.twig' %}
{% import 'macros/icon_helpers.html.twig' as icon %}


{% block body %}
<h1 class="mb-4">🎯 Shortcode-Button Details</h1>

<div class="card shadow-sm mb-4">
    <div class="card-body">
        <table class="table table-bordered align-middle">
            <tr><th>🔖 Tag</th><td><code>{{ shortcode_button.tag }}</code></td></tr>
            <tr><th>🔗 Route</th><td><code>{{ shortcode_button.route }}</code></td></tr>
            <tr><th>🏷️ Label</th><td>{{ shortcode_button.label }}</td></tr>
            <tr>
                <th>🖼️ Icon</th>
                <td>
                    {{ icon.renderIcon(shortcode_button.iconPath, shortcode_button.label) }}
                    
                </td>
            </tr>
            <tr><th>📦 Parameter</th><td><code>{{ shortcode_button.paramList }}</code></td></tr>
            <tr><th>👤 Erstellt von</th><td>{{ shortcode_button.createdBy }}</td></tr>
            <tr><th>📅 Erstellt am</th><td>{{ shortcode_button.createdAt|date('d.m.Y H:i') }}</td></tr>
            <tr><th>🛠️ Geändert am</th><td>{{ shortcode_button.updatedAt|date('d.m.Y H:i') }}</td></tr>
            <tr><th>📡 Aktiv</th><td>{{ shortcode_button.isActive ? '✅ Ja' : '❌ Nein' }}</td></tr>
        </table>
    </div>
</div>

<div class="mb-2">
    <label class="form-label fw-semibold">📁 Dateipfad:</label>
    <code>{{ shortcode_button.iconPath }}</code>
</div>

<h4 class="mb-2">📋 Copy & Paste</h4>
<div class="input-group mb-4 shadow-sm">
    <input type="text"
           class="form-control"
           id="shortcodeCopy"
           value='[[shortcode_button tag="{{ shortcode_button.tag }}" id=123]]'
           readonly>
    <button class="btn btn-outline-secondary"
            type="button"
            onclick="navigator.clipboard.writeText(document.getElementById('shortcodeCopy').value)">
        📋 Kopieren
    </button>
</div>

<a href="{{ path('app_shortcode_button_index') }}" class="btn btn-secondary">
    ⬅️ Zurück zur Übersicht
</a>
{% endblock %}

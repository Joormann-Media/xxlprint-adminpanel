<form method="post" action="{{ path('app_shortcode_button_delete', {'id': shortcode_button.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shortcode_button.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_admin_config_modules_delete', {'id': admin_config_module.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin_config_module.id) }}">
    <button class="btn">Delete</button>
</form>

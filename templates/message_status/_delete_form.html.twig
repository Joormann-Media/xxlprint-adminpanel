<form method="post" action="{{ path('app_message_status_delete', {'id': message_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_status.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Edit UserSession{% endblock %}

{% block body %}
    <h1>Edit UserSession</h1>

    {{ include('user_session/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_user_session_index') }}">back to list</a>

    {{ include('user_session/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_file_change_log_delete', {'id': file_change_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ file_change_log.id) }}">
    <button class="btn">Delete</button>
</form>

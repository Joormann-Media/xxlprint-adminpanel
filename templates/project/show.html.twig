{% extends 'base.html.twig' %}

{% block body %}
<div class="container py-5">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">📁 Projektdetails</h4>
            <div>
                <a href="{{ path('app_project_edit', {'id': project.id}) }}" class="btn btn-sm btn-warning me-2">✏️ Bearbeiten</a>
                <a href="{{ path('app_project_index') }}" class="btn btn-sm btn-outline-light">⬅️ Zurück</a>
                {{ include('project/_delete_form.html.twig') }}
            </div>
        </div>

        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-4">📝 Name:</dt>
                <dd class="col-sm-8">{{ project.name }}</dd>

                <dt class="col-sm-4">📅 Datum:</dt>
                <dd class="col-sm-8">{{ project.projectDate ? project.projectDate|date('d.m.Y') : '–' }}</dd>

                <dt class="col-sm-4">📌 Status:</dt>
                <dd class="col-sm-8">{{ project.projectStatus }}</dd>

                <dt class="col-sm-4">🧾 Version:</dt>
                <dd class="col-sm-8">{{ project.projectVersion }}</dd>

                <dt class="col-sm-4">🧠 Betreuer:</dt>
                <dd class="col-sm-8">
                    {% if project.supervisors|length %}
                        <ul>
                            {% for supervisor in project.supervisors %}
                                <li>{{ supervisor.username }}</li>
                            {% endfor %}
                        </ul>
                    {% else %}
                        <span class="text-muted">–</span>
                    {% endif %}
                </dd>

                <dt class="col-sm-4">📋 Kurzbeschreibung:</dt>
                <dd class="col-sm-8">{{ project.shortDescription }}</dd>

                <dt class="col-sm-4">📖 Langbeschreibung:</dt>
                <dd class="col-sm-8">{{ project.longDescription }}</dd>
            </dl>
        </div>
    </div>
</div>
{% endblock %}

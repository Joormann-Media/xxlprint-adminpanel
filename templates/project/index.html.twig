{% extends 'base.html.twig' %}

{% block body %}
<div class="container py-4">
    <div class="bg-light border rounded p-4 mb-4 shadow-sm">
        <h2 class="mb-0">📁 Projektübersicht</h2>
        <p class="text-muted">Alle angelegten Projekte im System.</p>
    </div>

    <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
        <a href="{{ path('app_project_new') }}" class="btn btn-success">
            <i class="bi bi-plus-circle me-1"></i> Neues Projekt
        </a>
    </div>

    {% if projects is not empty %}
        <div class="row g-4">
            {% for project in projects %}
                <div class="col-md-6 col-lg-4">
                    <div class="card shadow-sm h-100">
                        <div class="card-body">
                            <h5 class="card-title">{{ project.name }}</h5>
                            <p class="mb-1"><strong>📅 Datum:</strong> {{ project.projectDate ? project.projectDate|date('d.m.Y') : '–' }}</p>
                            <p class="mb-1"><strong>🧾 Version:</strong> {{ project.projectVersion ?: '–' }}</p>
                            <p class="mb-1"><strong>📌 Status:</strong> {{ project.projectStatus ?: '–' }}</p>
                            <p class="mb-1"><strong>📋 Kurzbeschreibung:</strong><br>{{ project.shortDescription }}</p>

                            <div class="card-footer d-flex justify-content-end gap-2 bg-transparent border-top-0">
                                <a href="{{ path('app_project_show', {'id': project.id}) }}" class="custom-button-icon" title="Anzeigen">
                                    <img src="/gfx/systemgfx/show.png" alt="Show">
                                </a>
                                <a href="{{ path('app_project_edit', {'id': project.id}) }}" class="custom-button-icon" title="Bearbeiten">
                                    <img src="/gfx/systemgfx/edit.png" alt="Edit">
                                </a>
                                <form method="post" action="{{ path('app_project_delete', {'id': project.id}) }}"
                                      onsubmit="return confirm('Möchtest du dieses Projekt wirklich löschen?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ project.id) }}">
                                    <button class="custom-button-icon" title="Löschen">
                                        <img src="/gfx/systemgfx/delete.png" alt="Delete">
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% else %}
        <div class="alert alert-warning text-center mt-4">
            🚫 Es wurden noch keine Projekte angelegt.
        </div>
    {% endif %}
</div>
{% endblock %}

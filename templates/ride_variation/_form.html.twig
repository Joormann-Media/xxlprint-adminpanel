{# templates/ride_variation/_form.html.twig #}
<div class="card shadow-sm border-0">
    <div class="card-body">
        {{ form_start(form) }}
        <div class="row g-3">
            <div class="col-md-6">
                {{ form_label(form.name, 'Name') }}
                {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="col-md-12">
                {{ form_label(form.description, 'Description') }}
                {{ form_widget(form.description, {'attr': {'class': 'form-control tinymce', 'rows': 3}}) }}
            </div>
        </div>
        <div class="text-end mt-4">
            <button class="btn btn-primary">💾 Save</button>
            <a href="{{ path('app_ride_variation_index') }}" class="btn btn-outline-secondary ms-2">⬅️ Back</a>
        </div>
        {{ form_end(form) }}
    </div>
</div>

{% extends 'base.html.twig' %}

{% block title %}RideVariation{% endblock %}

{% block body %}
    <h1>RideVariation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ride_variation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ride_variation.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ride_variation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ride_variation_index') }}">back to list</a>

    <a href="{{ path('app_ride_variation_edit', {'id': ride_variation.id}) }}">edit</a>

    {{ include('ride_variation/_delete_form.html.twig') }}
{% endblock %}

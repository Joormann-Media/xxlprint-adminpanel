{% extends 'base.html.twig' %}

{% block title %}BackupManager{% endblock %}

{% block body %}
    <h1>BackupManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ backup_manager.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ backup_manager.createdAt ? backup_manager.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ backup_manager.type }}</td>
            </tr>
            <tr>
                <th>PathSql</th>
                <td>{{ backup_manager.pathSql }}</td>
            </tr>
            <tr>
                <th>PathProject</th>
                <td>{{ backup_manager.pathProject }}</td>
            </tr>
            <tr>
                <th>GitRemoteStatus</th>
                <td>{{ backup_manager.gitRemoteStatus }}</td>
            </tr>
            <tr>
                <th>GitStatusTimestamp</th>
                <td>{{ backup_manager.gitStatusTimestamp ? backup_manager.gitStatusTimestamp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>GitStatusMessage</th>
                <td>{{ backup_manager.gitStatusMessage }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ backup_manager.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backup_manager_index') }}">back to list</a>

    <a href="{{ path('app_backup_manager_edit', {'id': backup_manager.id}) }}">edit</a>

    {{ include('backup_manager/_delete_form.html.twig') }}
{% endblock %}

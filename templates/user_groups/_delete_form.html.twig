<form method="post"
      action="{{ path('app_user_groups_delete', {'id': user_group.id}) }}"
      onsubmit="return confirm('❗ Willst du diese Benutzergruppe wirklich löschen? Dieser Vorgang kann nicht rückgängig gemacht werden.');"
      class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_group.id) }}">
    <button class="btn btn-danger btn-sm"
    data-bs-toggle="tooltip"
    title="Benutzergruppe unwiderruflich löschen">
🗑️ Löschen
</button>
</form>

<form method="post" action="{{ path('app_taxi_calculator_delete', {'id': taxi_calculator.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taxi_calculator.id) }}">
    <button class="btn">Delete</button>
</form>

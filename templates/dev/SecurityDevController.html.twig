{% extends 'base.html.twig' %}


{% block body %}
<a href="{{ path('app_admin_security_dev') }}?reset=1" class="btn btn-sm btn-danger">
    🧹 Fingerprint-Session zurücksetzen
</a>
<div class="container mt-4">
    <h1 class="mb-4">🛡️ Security Fingerprint Debug</h1>

    {% if error %}
        <div class="alert alert-danger">
            <strong>Fehler:</strong> {{ error }}
        </div>
    {% endif %}

    {% if raw_post %}
        <div class="mb-3">
            <h5>📦 Raw POST Data</h5>
            <pre>{{ raw_post|json_encode(constant('JSON_PRETTY_PRINT')) }}</pre>
        </div>
    {% endif %}

    {% if fingerprint %}
        <div class="mb-3">
            <h5>✅ Parsed Fingerprint Data</h5>
            <table class="table table-bordered">
                {% for key, value in fingerprint %}
                    <tr>
                        <th>{{ key }}</th>
                        <td>{{ value ?? '—' }}</td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    {% else %}
        <div class="alert alert-warning">Noch keine Fingerprint-Daten gespeichert.</div>
    {% endif %}
</div>

<script src="{{ asset('js/security-helper.js') }}"></script>
{% endblock %}

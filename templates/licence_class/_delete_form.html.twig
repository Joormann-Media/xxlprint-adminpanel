<form method="post" action="{{ path('app_licence_class_delete', {'id': licence_class.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ licence_class.id) }}">
    <button class="btn">Delete</button>
</form>

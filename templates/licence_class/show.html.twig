{% extends 'base.html.twig' %}

{% block title %}LicenceClass{% endblock %}

{% block body %}
    <h1>LicenceClass</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ licence_class.id }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ licence_class.shortName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ licence_class.description }}</td>
            </tr>
            <tr>
                <th>InfoBox</th>
                <td>{{ licence_class.infoBox }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_licence_class_index') }}">back to list</a>

    <a href="{{ path('app_licence_class_edit', {'id': licence_class.id}) }}">edit</a>

    {{ include('licence_class/_delete_form.html.twig') }}
{% endblock %}

<form method="post" action="{{ path('app_translation_value_delete', {'id': translation_value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ translation_value.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_message_recipient_delete', {'id': message_recipient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ message_recipient.id) }}">
    <button class="btn">Delete</button>
</form>

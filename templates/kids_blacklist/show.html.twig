{% extends 'base.html.twig' %}

{% block title %}KidsBlacklist{% endblock %}

{% block body %}
    <h1>KidsBlacklist</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kids_blacklist.id }}</td>
            </tr>
            <tr>
                <th>Reason</th>
                <td>{{ kids_blacklist.reason }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ kids_blacklist.createdAt ? kids_blacklist.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_kids_blacklist_index') }}">back to list</a>

    <a href="{{ path('app_kids_blacklist_edit', {'id': kids_blacklist.id}) }}">edit</a>

    {{ include('kids_blacklist/_delete_form.html.twig') }}
{% endblock %}

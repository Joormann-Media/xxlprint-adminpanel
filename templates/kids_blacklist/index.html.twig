{% extends 'base.html.twig' %}

{% block title %}KidsBlacklist index{% endblock %}

{% block body %}
    <h1>KidsBlacklist index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Reason</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for kids_blacklist in kids_blacklists %}
            <tr>
                <td>{{ kids_blacklist.id }}</td>
                <td>{{ kids_blacklist.reason }}</td>
                <td>{{ kids_blacklist.createdAt ? kids_blacklist.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_kids_blacklist_show', {'id': kids_blacklist.id}) }}">show</a>
                    <a href="{{ path('app_kids_blacklist_edit', {'id': kids_blacklist.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_kids_blacklist_new') }}">Create new</a>
{% endblock %}

<form method="post" action="{{ path('app_contact_person_delete', {'id': contact_person.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_person.id) }}">
    <button class="btn">Delete</button>
</form>

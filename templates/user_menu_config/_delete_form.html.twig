<form method="post" action="{{ path('app_user_menu_config_delete', {'id': user_menu_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_menu_config.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_api_documentation_delete', {'id': api_documentation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ api_documentation.id) }}">
    <button class="btn">Delete</button>
</form>

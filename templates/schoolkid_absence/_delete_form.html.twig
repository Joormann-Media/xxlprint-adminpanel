<form method="post" action="{{ path('app_schoolkid_absence_delete', {'id': schoolkid_absence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ schoolkid_absence.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_vacation_manager_delete', {'id': vacation_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vacation_manager.id) }}">
    <button class="custom-button-icon"><img src="/gfx/systemgfx/delete.png" alt="Löschen" ></button>
</form>

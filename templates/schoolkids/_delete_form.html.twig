<form method="post" action="{{ path('app_schoolkids_delete', {'id': schoolkid.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ schoolkid.id) }}">
    <button class="btn">Delete</button>
</form>

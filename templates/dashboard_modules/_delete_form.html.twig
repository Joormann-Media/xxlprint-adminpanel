<form method="post" action="{{ path('app_dashboard_modules_delete', {'id': dashboard_module.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dashboard_module.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}


{% block body %}
<div class="container py-4">
    <div class="card shadow-sm border-0">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">📊 Dashboard-Modul Details</h4>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-6">
                    <p><strong>ID:</strong> {{ dashboard_module.id }}</p>
                    <p><strong>Name:</strong> {{ dashboard_module.name }}</p>
                    <p><strong>Beschreibung:</strong><br>{{ dashboard_module.description|default('–') }}</p>
                    <p><strong>Aktiv:</strong> {{ dashboard_module.isActive ? '✅ Ja' : '❌ Nein' }}</p>
                    <p><strong>Minimale Rolle:</strong> {{ dashboard_module.minRole }}</p>
                </div>
                <div class="col-md-6">
                    <p><strong>Erstellt am:</strong> {{ dashboard_module.createdAt ? dashboard_module.createdAt|date('d.m.Y H:i') : '–' }}</p>
                    <p><strong>Erstellt von:</strong> {{ dashboard_module.createdBy ?: '–' }}</p>
                    <p><strong>Aktualisiert am:</strong> {{ dashboard_module.updatedAt ? dashboard_module.updatedAt|date('d.m.Y H:i') : '–' }}</p>
                    <p><strong>Aktualisiert von:</strong> {{ dashboard_module.updatedBy ?: '–' }}</p>
                </div>
            </div>

            {% if dashboard_module.content %}
                <hr>
                <p><strong>Inhalt:</strong></p>
                <div class="border p-3 bg-light rounded small">
                    {{ dashboard_module.content|raw }}
                </div>
            {% endif %}
        </div>
        <div class="card-footer d-flex justify-content-between align-items-center">
            <a href="{{ path('app_dashboard_modules_index') }}" class="btn btn-outline-secondary">
                ⬅️ Zurück zur Übersicht
            </a>
            <div class="d-flex gap-2">
                <a href="{{ path('app_dashboard_modules_edit', {'id': dashboard_module.id}) }}" class="btn btn-warning">
                    ✏️ Bearbeiten
                </a>
                {{ include('dashboard_modules/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}

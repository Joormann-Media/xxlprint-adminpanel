{% extends 'base.html.twig' %}

{% block title %}EmployeeVacation{% endblock %}

{% block body %}
    <h1>EmployeeVacation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employee_vacation.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ employee_vacation.year }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ employee_vacation.total }}</td>
            </tr>
            <tr>
                <th>Used</th>
                <td>{{ employee_vacation.used }}</td>
            </tr>
            <tr>
                <th>Remaining</th>
                <td>{{ employee_vacation.remaining }}</td>
            </tr>
            <tr>
                <th>Remark</th>
                <td>{{ employee_vacation.remark }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_employee_vacation_index') }}">back to list</a>

    <a href="{{ path('app_employee_vacation_edit', {'id': employee_vacation.id}) }}">edit</a>

    {{ include('employee_vacation/_delete_form.html.twig') }}
{% endblock %}

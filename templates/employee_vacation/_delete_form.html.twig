<form method="post" action="{{ path('app_employee_vacation_delete', {'id': employee_vacation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_vacation.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_online_status_config_delete', {'id': online_status_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ online_status_config.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_geo_coordinate_delete', {'id': geo_coordinate.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ geo_coordinate.id) }}">
    <button class="btn">Delete</button>
</form>

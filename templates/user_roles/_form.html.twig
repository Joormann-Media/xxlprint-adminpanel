<div class="container py-5">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h2 class="mb-4">🎭 Benutzerrolle verwalten</h2>

            {{ form_start(form, {'attr': {'class': 'row g-3'}}) }}

                <div class="col-md-6">
                    {{ form_label(form.roleName) }}
                    {{ form_widget(form.roleName, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.roleTag) }}
                    {{ form_widget(form.roleTag, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.roleCreate) }}
                    {{ form_widget(form.roleCreate, {'attr': {'class': 'form-control', 'value': "now"|date("Y-m-d H:i:s"), 'readonly': 'readonly'}}) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.roleCreateBy) }}
                    {{ form_widget(form.roleCreateBy, {'attr': {'class': 'form-control', 'value': app.user.username, 'readonly': 'readonly'}}) }}
                </div>

                <div class="col-md-12">
                    {{ form_label(form.roleDescription) }}
                    {{ form_widget(form.roleDescription) }}
                </div>

                <div class="col-md-6">
                    {{ form_label(form.hierarchy) }}
                    {{ form_widget(form.hierarchy, {'attr': {'class': 'form-control', 'readonly': 'readonly'}}) }}
                </div>

                <div class="col-12 text-end mt-3">
                    <button class="btn btn-primary">💾 Speichern</button>
                    <a href="{{ path('app_user_roles_index') }}" class="btn btn-secondary ms-2">⬅️ Zurück</a>
                </div>

            {{ form_end(form) }}
        </div>
    </div>
</div>

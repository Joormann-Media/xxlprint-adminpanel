{% extends 'base.html.twig' %}


{% block body %}
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu_sub_menu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ menu_sub_menu.name }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ menu_sub_menu.status }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ menu_sub_menu.createAt ? menu_sub_menu.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ menu_sub_menu.updateAt ? menu_sub_menu.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateBy</th>
                <td>{{ menu_sub_menu.updateBy }}</td>
            </tr>
            <tr>
                <th>MinRole</th>
                <td>{{ menu_sub_menu.minRole }}</td>
            </tr>
            <tr>
                <th>ParentId</th>
                <td>{{ menu_sub_menu.parentId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_menu_sub_menu_index') }}">back to list</a>

    <a href="{{ path('app_menu_sub_menu_edit', {'id': menu_sub_menu.id}) }}">edit</a>

    {{ include('menu_sub_menu/_delete_form.html.twig') }}
{% endblock %}

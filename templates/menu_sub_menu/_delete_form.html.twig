<form method="post" action="{{ path('app_menu_sub_menu_delete', {'id': menu_sub_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu_sub_menu.id) }}">
    <button class="btn">Delete</button>
</form>

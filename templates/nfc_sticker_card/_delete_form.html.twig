<form method="post" action="{{ path('app_nfc_sticker_card_delete', {'id': nfc_sticker_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nfc_sticker_card.id) }}">
    <button class="btn">Delete</button>
</form>

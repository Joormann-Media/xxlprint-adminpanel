<form method="post" action="{{ path('app_user_online_status_delete', {'id': user_online_status.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_online_status.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Token anzeigen{% endblock %}

{% block body %}
<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="mb-0">🔐 API-Token anzeigen</h2>
        <div>
            <a href="{{ path('app_api_token_index') }}" class="btn btn-secondary">← Zurück zur Liste</a>
            <a href="{{ path('app_api_token_edit', {'id': api_token.id}) }}" class="btn btn-primary ms-2">✏️ Bearbeiten</a>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-3">Token</dt>
                <dd class="col-sm-9"><code>{{ api_token.token }}</code></dd>

                <dt class="col-sm-3">Typ</dt>
                <dd class="col-sm-9">{{ api_token.type }}</dd>

                <dt class="col-sm-3">Erstellt am</dt>
                <dd class="col-sm-9">{{ api_token.createdAt|date('Y-m-d H:i') }}</dd>

                <dt class="col-sm-3">Ablaufdatum</dt>
                <dd class="col-sm-9">{{ api_token.expiresAt|date('Y-m-d H:i') }}</dd>

                <dt class="col-sm-3">Verwendet</dt>
                <dd class="col-sm-9">
                    {% if api_token.used %}
                        ✅ Ja
                    {% else %}
                        ❌ Nein
                    {% endif %}
                </dd>

                <dt class="col-sm-3">Partnerfirma</dt>
                <dd class="col-sm-9">
                    {% if api_token.partnerCompany %}
                        <strong>{{ api_token.partnerCompany.name }}</strong><br>
                        <small class="text-muted">{{ api_token.partnerCompany.slug }}</small>
                    {% else %}
                        <em class="text-muted">–</em>
                    {% endif %}
                </dd>
            </dl>
        </div>
    </div>
</div>
{% endblock %}

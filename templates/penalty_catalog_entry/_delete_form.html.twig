<form method="post" action="{{ path('app_penalty_catalog_entry_delete', {'id': penalty_catalog_entry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ penalty_catalog_entry.id) }}">
    <button class="btn">Delete</button>
</form>

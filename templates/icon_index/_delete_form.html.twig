<form method="post" action="{{ path('app_icon_index_delete', {'id': icon_index.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ icon_index.id) }}">
    <button class="btn">Delete</button>
</form>

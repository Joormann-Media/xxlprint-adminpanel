{% extends 'base.html.twig' %}

{% block title %}DoctypeManager{% endblock %}

{% block body %}
    <h1>DoctypeManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ doctype_manager.id }}</td>
            </tr>
            <tr>
                <th>DoctypeName</th>
                <td>{{ doctype_manager.doctypeName }}</td>
            </tr>
            <tr>
                <th>DoctypeType</th>
                <td>{{ doctype_manager.doctypeType }}</td>
            </tr>
            <tr>
                <th>DoctypeAdd</th>
                <td>{{ doctype_manager.doctypeAdd ? doctype_manager.doctypeAdd|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_doctype_manager_index') }}">back to list</a>

    <a href="{{ path('app_doctype_manager_edit', {'id': doctype_manager.id}) }}">edit</a>

    {{ include('doctype_manager/_delete_form.html.twig') }}
{% endblock %}

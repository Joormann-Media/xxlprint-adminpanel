<form method="post" action="{{ path('app_school_tour_stop_delete', {'id': school_tour_stop.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_tour_stop.id) }}">
    <button class="btn">Delete</button>
</form>

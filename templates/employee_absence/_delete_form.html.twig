<form method="post" action="{{ path('app_employee_absence_delete', {'id': employee_absence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ employee_absence.id) }}">
    <button class="btn">Delete</button>
</form>

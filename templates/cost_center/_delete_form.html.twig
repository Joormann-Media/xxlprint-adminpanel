<form method="post" action="{{ path('app_cost_center_delete', {'id': cost_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cost_center.id) }}">
    <button class="btn">Delete</button>
</form>

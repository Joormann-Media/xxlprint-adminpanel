<form method="post" action="{{ path('app_language_file_delete', {'id': language_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ language_file.id) }}">
    <button class="btn">Delete</button>
</form>

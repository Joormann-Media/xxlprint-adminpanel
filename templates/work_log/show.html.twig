{% extends 'base.html.twig' %}

{% block title %}WorkLog{% endblock %}

{% block body %}
    <h1>WorkLog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_log.id }}</td>
            </tr>
            <tr>
                <th>ClockInAt</th>
                <td>{{ work_log.clockInAt ? work_log.clockInAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ work_log.location }}</td>
            </tr>
            <tr>
                <th>Method</th>
                <td>{{ work_log.method }}</td>
            </tr>
            <tr>
                <th>DeviceUid</th>
                <td>{{ work_log.deviceUid }}</td>
            </tr>
            <tr>
                <th>Source</th>
                <td>{{ work_log.source }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ work_log.createdAt ? work_log.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_work_log_index') }}">back to list</a>

    <a href="{{ path('app_work_log_edit', {'id': work_log.id}) }}">edit</a>

    {{ include('work_log/_delete_form.html.twig') }}
{% endblock %}

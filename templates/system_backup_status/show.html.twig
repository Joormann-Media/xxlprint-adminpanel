{% extends 'base.html.twig' %}

{% block title %}SystemBackupStatus{% endblock %}

{% block body %}
    <h1>SystemBackupStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ system_backup_status.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ system_backup_status.createdAt ? system_backup_status.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ system_backup_status.type }}</td>
            </tr>
            <tr>
                <th>Mode</th>
                <td>{{ system_backup_status.mode }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ system_backup_status.path }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ system_backup_status.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_system_backup_status_index') }}">back to list</a>

    <a href="{{ path('app_system_backup_status_edit', {'id': system_backup_status.id}) }}">edit</a>

    {{ include('system_backup_status/_delete_form.html.twig') }}
{% endblock %}

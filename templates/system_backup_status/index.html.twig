{% extends 'base.html.twig' %}

{% block title %}SystemBackupStatus index{% endblock %}

{% block body %}
    <h1>SystemBackupStatus index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CreatedAt</th>
                <th>Type</th>
                <th>Mode</th>
                <th>Path</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for system_backup_status in system_backup_statuses %}
            <tr>
                <td>{{ system_backup_status.id }}</td>
                <td>{{ system_backup_status.createdAt ? system_backup_status.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ system_backup_status.type }}</td>
                <td>{{ system_backup_status.mode }}</td>
                <td>{{ system_backup_status.path }}</td>
                <td>{{ system_backup_status.status }}</td>
                <td>
                    <a href="{{ path('app_system_backup_status_show', {'id': system_backup_status.id}) }}">show</a>
                    <a href="{{ path('app_system_backup_status_edit', {'id': system_backup_status.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_system_backup_status_new') }}">Create new</a>
{% endblock %}

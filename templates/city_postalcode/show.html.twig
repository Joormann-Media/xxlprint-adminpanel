{% extends 'base.html.twig' %}

{% block title %}CityPostalcode{% endblock %}

{% block body %}
    <h1>CityPostalcode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ city_postalcode.id }}</td>
            </tr>
            <tr>
                <th>Postcode</th>
                <td>{{ city_postalcode.postcode }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ city_postalcode.city }}</td>
            </tr>
            <tr>
                <th>State</th>
                <td>{{ city_postalcode.state }}</td>
            </tr>
            <tr>
                <th>Lat</th>
                <td>{{ city_postalcode.lat }}</td>
            </tr>
            <tr>
                <th>Lon</th>
                <td>{{ city_postalcode.lon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_city_postalcode_index') }}">back to list</a>

    <a href="{{ path('app_city_postalcode_edit', {'id': city_postalcode.id}) }}">edit</a>

    {{ include('city_postalcode/_delete_form.html.twig') }}
{% endblock %}

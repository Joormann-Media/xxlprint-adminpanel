<form method="post" action="{{ path('app_city_postalcode_delete', {'id': city_postalcode.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ city_postalcode.id) }}">
    <button class="btn">Delete</button>
</form>

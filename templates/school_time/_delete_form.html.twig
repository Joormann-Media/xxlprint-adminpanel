<form method="post" action="{{ path('app_school_time_delete', {'id': school_time.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ school_time.id) }}">
    <button class="btn">Delete</button>
</form>

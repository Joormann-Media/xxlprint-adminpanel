<form method="post" action="{{ path('app_email_signature_delete', {'id': e_mail_signature.id}) }}" onsubmit="return confirm('Sind Sie sicher, dass Sie dieses Element löschen möchten?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e_mail_signature.id) }}">
    <button class="btn btn-danger btn-sm" type="submit">
        <i class="bi bi-trash"></i> Löschen
    </button>
</form>

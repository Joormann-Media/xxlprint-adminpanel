<form method="post" action="{{ path('app_user_device_log_delete', {'id': user_device_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_device_log.id) }}">
    <button class="btn">Delete</button>
</form>

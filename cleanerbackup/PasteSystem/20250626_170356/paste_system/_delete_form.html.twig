<form method="post" action="{{ path('app_paste_system_delete', {'id': paste_system.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paste_system.id) }}">
    <button class="btn">Delete</button>
</form>

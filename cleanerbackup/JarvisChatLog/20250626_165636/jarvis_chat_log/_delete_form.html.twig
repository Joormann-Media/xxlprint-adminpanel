<form method="post" action="{{ path('app_jarvis_chat_log_delete', {'id': jarvis_chat_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ jarvis_chat_log.id) }}">
    <button class="btn">Delete</button>
</form>

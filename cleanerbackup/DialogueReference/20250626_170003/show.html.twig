{% extends 'base.html.twig' %}

{% block title %}DialogueReference{% endblock %}

{% block body %}
    <h1>DialogueReference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dialogue_reference.id }}</td>
            </tr>
            <tr>
                <th>RoomId</th>
                <td>{{ dialogue_reference.roomId }}</td>
            </tr>
            <tr>
                <th>DialogId</th>
                <td>{{ dialogue_reference.dialogId }}</td>
            </tr>
            <tr>
                <th>DialogText</th>
                <td>{{ dialogue_reference.dialogText }}</td>
            </tr>
            <tr>
                <th>DialogLang</th>
                <td>{{ dialogue_reference.dialogLang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dialogue_reference_index') }}">back to list</a>

    <a href="{{ path('app_dialogue_reference_edit', {'id': dialogue_reference.id}) }}">edit</a>

    {{ include('dialogue_reference/_delete_form.html.twig') }}
{% endblock %}
